# Copiá este archivo a ".env" en el host (NO lo commitees con secretos reales).

# Database (si usás el servicio `db` del compose)
POSTGRES_DB=agents_wf_builder
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@db:5432/agents_wf_builder

# App
PORT=3000

# Better Auth
BETTER_AUTH_SECRET=change-me
BETTER_AUTH_URL=http://localhost:3000

# AI Gateway (para generación de workflows)
AI_GATEWAY_API_KEY=change-me


