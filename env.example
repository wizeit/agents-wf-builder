# Copiá este archivo a ".env" en el host (NO lo commitees con secretos reales).

# Database (usa el Postgres compartido en docker-network-data)
# Primero crear la DB: CREATE DATABASE wf_builder;
DATABASE_URL=postgresql://postgres:postgres@data-postgres:5432/wf_builder

# Better Auth
BETTER_AUTH_SECRET=change-me
BETTER_AUTH_URL=http://localhost:3004

# AI Gateway (para generación de workflows)
AI_GATEWAY_API_KEY=change-me
